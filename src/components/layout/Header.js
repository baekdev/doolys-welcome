import React from 'react';
import '../../style/style.scss';

const Header = React.memo(() => {

  return (
    <header>
      <nav>
        <h1>둘리 & 도우너 어서오고 짤 생성기</h1>
      </nav>
    </header>
  );
});

export default Header;
